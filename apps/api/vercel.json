{
  "buildCommand": "npm run build && mkdir -p public && echo '{\"ok\":true}' > public/index.json",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
